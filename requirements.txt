# AskTennis - Unified Requirements
# This file contains all dependencies needed for both local and Render deployment
# Install with: pip install -r requirements.txt

# Core Application Dependencies
streamlit>=1.28.0
pandas>=2.0.0
sqlalchemy>=2.0.0

# AI/LLM Dependencies (LangChain & LangGraph)
langchain>=0.1.0
langchain-community>=0.0.20
langchain-google-genai>=1.0.0
langgraph>=0.0.40

# Database Dependencies
psycopg2-binary>=2.9.0  # PostgreSQL adapter for Render deployment
# Note: sqlite3 is built into Python, no need to install separately

# Data Processing & Analysis
numpy>=1.21.0
scikit-learn>=1.1.0

# Visualization Dependencies
spacy>=3.4.0
plotly>=5.15.0
lxml>=4.9.0

# Environment & Configuration
python-dotenv>=1.0.0  # For environment variables (.env files)

# Additional Dependencies for Enhanced Features
# Note: These may be needed for specific database operations
# Uncomment if needed for your specific use case
# openpyxl>=3.0.0  # For Excel file processing
# requests>=2.28.0  # For API calls